# Default values for astar.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: astarsearch:latest

problem: libs/problems/boarding.yaml
